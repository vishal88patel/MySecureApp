import 'package:flutter/material.dart';

class ApplyLoanInfoScreen extends StatelessWidget {
  const ApplyLoanInfoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
